#!/usr/bin/python3
# Comments allows to execute it
# import dis
def magic_calculation(a, b):
    if a < b:
        return c
    elif c > b:
        return a + b
    else:
        return ((a * b) - c)
# dis.dis(magic_calculation)
